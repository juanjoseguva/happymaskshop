<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/majorasmask"
        android:label="@string/app_name"
        android:roundIcon="@drawable/majorasmask"
        android:supportsRtl="true"
        android:theme="@style/Theme.HappyMaskShopOfHorrors"
        tools:targetApi="31">
        <activity android:name=".activities.MainActivity" />
        <activity android:name=".activities.DragonActivity" />
        <activity android:name=".activities.SearchActivity" />
        <activity android:name=".activities.CreateAccountActivity" />
        <activity android:name=".activities.ChangePasswordActivity" />
        <activity android:name=".activities.ChangeAddressActivity" />
        <activity android:name=".activities.PresentItemActivity" />
        <activity android:name=".activities.CheckoutActivity" />
        <activity android:name=".activities.AccountActivity" />
        <activity android:name=".activities.DisplayOrdersActivity" />
        <activity android:name=".activities.BannedActivity" />
        <activity android:name=".activities.BarterActivity" />
        <activity android:name=".activities.WorkActivity" />
        <activity android:name=".activities.ManageClientsActivity" />
        <activity android:name=".activities.ManageOrdersActivity" />
        <activity
            android:name=".activities.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <meta-data
                android:name="android.app.lib_name"
                android:value="" />
        </activity>
    </application>

</manifest>